# Supabase (optional — demo mode works without these)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# OpenWeather (optional — simulated weather fallback)
VITE_OPENWEATHER_API_KEY=

# OpenAI (optional — explanation layer only; policy decisions stay deterministic)
VITE_OPENAI_API_KEY=
